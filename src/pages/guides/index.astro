---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArticleCard from '../../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

const allArticles = await getCollection('articles');
const articles = allArticles
  .filter((a) => a.data.category === 'care' || a.data.category === 'planning')
  .sort((a, b) => b.data.publishDate.getTime() - a.data.publishDate.getTime());
---

<BaseLayout
  title="Vegetable Garden Guides: Soil, Care, and Planning"
  description="Practical growing guides  -  soil pH, hardiness zones, succession planting, IPM, and garden layout comparisons. The reference layer for growing food well."
>
  <div class="page-header">
    <div class="container">
      <h1>Guides</h1>
      <p>Soil, care, zones, and planning  -  the reference layer for growing food well.</p>
    </div>
  </div>

  <div class="container section-page">
    {articles.length === 0 ? (
      <p class="empty">Articles coming soon.</p>
    ) : (
      <div class="articles-grid">
        {articles.map((article) => (
          <ArticleCard
            slug={article.id}
            title={article.data.title}
            description={article.data.description}
            category={article.data.category}
            publishDate={article.data.publishDate}
            featured={article.data.featured}
            sectionPath="/guides"
          />
        ))}
      </div>
    )}
  </div>
</BaseLayout>

<style>
  .section-page {
    padding: var(--space-xl) var(--space-l);
  }

  .articles-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-l);
  }

  .empty {
    color: var(--color-text-muted);
    font-size: var(--text-body);
    padding: var(--space-xl) 0;
  }

  @media (max-width: 900px) {
    .articles-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 600px) {
    .articles-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
